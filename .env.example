# Copy to .env and fill in. Never commit .env.
#
# Anthropic: leave unset — use Claude Max via `claude /login` for orchestrator + Remote Control.
# Do NOT put ANTHROPIC_API_KEY here if you want Max subscription billing in Claude Code.
OPENAI_API_KEY=
GOOGLE_API_KEY=
XAI_API_KEY=