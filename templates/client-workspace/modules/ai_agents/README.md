# AI agents module

Custom agent frameworks (e.g., Hermes-style builds).

Suggested layout:
- `core/` — state machine and memory
- `prompts/` — system personas and tools
- `requirements.txt` — Python dependencies
